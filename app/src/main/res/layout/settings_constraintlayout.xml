<?xml version="1.0" encoding="utf-8"?>
<com.liweiyap.pomodoro.ui.SettingsConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/settingsNameTextView"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/settingsValueSpinner"
        app:layout_constraintHorizontal_weight="3"
        android:gravity="center"
        android:textSize="20sp" />

    <Spinner
        android:id="@+id/settingsValueSpinner"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toRightOf="@id/settingsNameTextView"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintHorizontal_weight="1"
        android:spinnerMode="dropdown" />

</com.liweiyap.pomodoro.ui.SettingsConstraintLayout>