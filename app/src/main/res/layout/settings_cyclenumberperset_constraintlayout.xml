<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/settingsCycleNumberPerSetTextView"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/settingsCycleNumberPerSetSpinner"
        app:layout_constraintHorizontal_weight="3"
        android:gravity="center"
        android:textSize="20sp"
        android:text="@string/settingsCycleNumberPerSetText" />

    <Spinner
        android:id="@+id/settingsCycleNumberPerSetSpinner"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toRightOf="@id/settingsCycleNumberPerSetTextView"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintHorizontal_weight="1"
        android:spinnerMode="dropdown" />

</androidx.constraintlayout.widget.ConstraintLayout>